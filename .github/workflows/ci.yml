name: CI

on:
  push:
  pull_request:

# Least privilege: jobs only need to read the repo. CodeQL (separate workflow)
# requests the extra scopes it needs.
permissions:
  contents: read

# Cancel superseded runs on the same ref to save CI minutes — but never on the
# default branch, so every commit that lands on main keeps a completed run on
# record (a fast follow-up push must not cancel main's test/security history).
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  lint:
    name: Lint (ruff, py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
      - run: uv python install ${{ matrix.python-version }}
      - run: uv sync --extra dev --extra tui --python ${{ matrix.python-version }}
      - name: Ruff lint
        run: uv run ruff check --output-format=github .

  typecheck:
    name: Type check (mypy strict, py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
      - run: uv python install ${{ matrix.python-version }}
      # Resolve per interpreter so version-marked deps / type stubs are checked
      # under each supported Python, not just one.
      - run: uv sync --extra dev --extra tui --python ${{ matrix.python-version }}
      - name: mypy
        run: uv run mypy src

  test:
    name: Test (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
      - run: uv python install ${{ matrix.python-version }}
      # --extra tui is required: the TUI tests import textual/selectolax.
      - run: uv sync --extra dev --extra tui --python ${{ matrix.python-version }}
      - name: Pytest with coverage gate
        run: >
          uv run pytest -q
          --cov=perplexity_agent
          --cov-report=term-missing
          --cov-report=xml
      - name: Upload coverage report
        if: matrix.python-version == '3.12'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-xml
          path: coverage.xml
          if-no-files-found: error

  conformance:
    name: MCP conformance (live stdio)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
      - run: uv python install 3.12
      - run: uv sync --extra dev --extra tui --python 3.12
      - name: Conformance suite
        # mcp-conformance is a private repo, so this job is a graceful no-op until
        # MCP_CONFORMANCE_TOKEN (a PAT with read access to it) is set in the repo's
        # Actions secrets. Then it boots perplexity-agent over stdio and asserts the
        # baseline contracts (see conformance.toml; a dummy API key lets it start).
        env:
          MCP_CONFORMANCE_TOKEN: ${{ secrets.MCP_CONFORMANCE_TOKEN }}
        run: |
          if [ -z "$MCP_CONFORMANCE_TOKEN" ]; then
            echo "MCP_CONFORMANCE_TOKEN not set — skipping conformance suite."
            exit 0
          fi
          uv pip install "git+https://x-access-token:${MCP_CONFORMANCE_TOKEN}@github.com/CryptoJones/mcp-conformance.git"
          uv run mcp-conformance run -- -q

  core-install:
    name: Core install (no tui extra)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
      - run: uv python install 3.12
      # Deliberately NO --extra tui: this is what plain `pip install
      # perplexity-agent` users get. Catches an accidental top-level
      # textual/selectolax import sneaking into a core module.
      - run: uv sync --extra dev --python 3.12
      - name: Import core modules without the tui extra
        run: >
          uv run python -c "import perplexity_agent.__main__,
          perplexity_agent.server, perplexity_agent.client,
          perplexity_agent.research, perplexity_agent.assistant,
          perplexity_agent.fetch, perplexity_agent.tasks"
      - name: Run non-tui tests against the core env
        run: uv run pytest -q --ignore=tests/test_tui.py

  security:
    name: Security (audit + secrets, py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Full history so gitleaks can scan every commit, not just the worktree.
          fetch-depth: 0
      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
      - run: uv python install ${{ matrix.python-version }}
      # Resolve per interpreter so a CVE in a version-marked dependency is
      # audited under every supported Python, not just one.
      - run: uv sync --extra dev --extra tui --python ${{ matrix.python-version }}
      - name: Dependency vulnerability audit (pip-audit)
        run: uv run pip-audit
      - name: Secret scan (gitleaks, full history)
        # The repo contents are identical across the matrix; scan once.
        # safe.directory: the mounted repo is owned by the runner uid, not the
        # container's root, and git refuses "dubious ownership" without it.
        if: matrix.python-version == '3.12'
        run: |
          docker run --rm -v "${{ github.workspace }}:/repo" \
            --entrypoint sh ghcr.io/gitleaks/gitleaks:v8.30.1 \
            -c "git config --global --add safe.directory /repo && \
                gitleaks git --redact --verbose /repo"
